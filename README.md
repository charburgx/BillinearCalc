# BillinearCalc
A tool used to calculate information about integer billinear sequences modulo an integer m
# Usage
Nodejs is required. Simply run ``npm install``, followed by ``npm start`` to generate text, json, and tex outputs. The files will output in the main directory.
